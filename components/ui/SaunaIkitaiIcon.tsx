import React from 'react';

export function SaunaIkitaiIcon({ className }: { className?: string }) {
  return (
    <span
      className={`inline-flex shrink-0 items-center justify-center rounded-full border border-current text-[10px] font-bold leading-none ${className ?? ''}`}
    >
      サ
    </span>
  );
}
