DATABASE_URL="file:./prisma/dev.db"
ADMIN_PASSWORD=
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=

# Notion連携 (ブログ記事の同期)
# データベースには タイトル(Title) / 日付(Date, 作成日時でも可) / カテゴリ(Category) / タグ(Tags) / 画像(ImageUrl) を用意する
NOTION_API_KEY=
NOTION_DATABASE_ID=
